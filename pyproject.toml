[project]
name = "humanizer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "flask>=3.0",
  "requests>=2.31",
  "python-dotenv>=1.0",
  "nltk>=3.9",
  "pyyaml>=6.0",
  "textstat>=0.7",
]

[tool.ruff]
line-length = 100
